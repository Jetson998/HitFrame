// 运行时前端配置（R10）。此为默认空模板，随构建产物发布。
// 部署时按环境覆盖本文件（例如演示环境注入只读 token）：
//   window.__HF_CONFIG__ = { apiToken: "<部署注入>" };
// 生产建议：不注入 token，令用户以 TokenGate 粘贴——引擎密钥永不到前端。
window.__HF_CONFIG__ = window.__HF_CONFIG__ || {};
